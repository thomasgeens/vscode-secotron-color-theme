# Exclude everything by default, then explicitly include only what the extension needs.
# Any new file added to the repo is automatically excluded from the VSIX.
**

!LICENSE
!LICENSE.txt
!CHANGELOG.md
!README.md
!package.json
!color_palette.png
!example.png
!logo.png
!themes/**
